# نظام تسجيل المشرفين ومدراء المدارس - دليل التشغيل

## 🎯 ملخص النظام

تم تطوير نظام شامل لتسجيل المشرفين ومدراء المدارس باستخدام نظام الدعوات المبني على التوكنز (Token-based Invitations).

## 🏗️ المكونات المطورة

### 1. Backend API (Laravel)

#### أ) Controller Methods (AuthController.php):
- `validateRegistrationToken()` - التحقق من صحة توكن التسجيل
- `registerSupervisor()` - تسجيل مشرف جديد
- `registerPrincipal()` - تسجيل مدير مدرسة جديد
- `getSupervisorLinks()` - جلب جميع روابط الدعوات
- `createSupervisorLink()` - إنشاء رابط دعوة جديد
- `updateSupervisorLink()` - تحديث رابط دعوة
- `deleteSupervisorLink()` - حذف رابط دعوة
- `getSupervisorLinksStatistics()` - إحصائيات الروابط

#### ب) Database Migrations:
- `supervisor_link_usage` - جدول تتبع استخدام الروابط
- إضافة حقول `status` و `supervisor_id` و `phone` لجدول المستخدمين

#### ج) API Routes:
- `GET /api/auth/validate-token/{token}` - التحقق من التوكن
- `POST /api/auth/register-supervisor` - تسجيل مشرف
- `POST /api/auth/register-principal` - تسجيل مدير مدرسة
- `GET /api/admin/supervisor-links` - إدارة الروابط (أدمن)
- `POST /api/admin/supervisor-links` - إنشاء رابط (أدمن)
- `PUT /api/admin/supervisor-links/{id}` - تحديث رابط (أدمن)
- `DELETE /api/admin/supervisor-links/{id}` - حذف رابط (أدمن)

### 2. Frontend Components (React)

#### أ) صفحات التسجيل:
- `SupervisorRegistration.jsx` - صفحة تسجيل المشرفين
- `PrincipalRegistration.jsx` - صفحة تسجيل مدراء المدارس

#### ب) إدارة الدعوات:
- `SupervisorLinksManagement.jsx` - واجهة إدارة الدعوات للأدمن

#### ج) Services:
- `authService.js` - خدمات التسجيل المبنية على التوكن
- `adminService.js` - خدمات إدارة الدعوات

#### د) Routes:
- `/register/supervisor/:token` - تسجيل مشرف
- `/register/principal?supervisor_token=TOKEN` - تسجيل مدير مدرسة

## 🔧 آلية العمل

### 1. تسجيل المشرفين
```
URL: /register/supervisor/XYZ123

الحقول:
- الاسم الكامل
- البريد الإلكتروني  
- رقم الهاتف (اختياري)
- كلمة المرور + تأكيدها

الآلية:
1. التحقق من التوكن: إذا غير صالح → رفض
2. إضافة سجل في DB: status = pending
3. إشعار الـ Admin للمراجعة → بعد الموافقة يتحول إلى active
```

### 2. تسجيل مدراء المدارس
```
URL: /register/principal?supervisor_token=ABC123

الحقول:
- الاسم الكامل
- البريد الإلكتروني
- كلمة المرور + تأكيدها

الآلية:
1. التحقق من supervisor_token
2. ربط المدير بالمشرف المسؤول مباشرة
3. إمكانية وضعه Pending حتى موافقة المشرف أو تفعيل مباشر
```

### 3. إدارة الدعوات (للـ Admin)
```
URL: /admin/supervisor-links

الوظائف:
- إنشاء روابط دعوة جديدة مع صلاحية (مدة، عدد استخدامات)
- إدارة حالة الروابط (تفعيل / تعطيل)
- تتبع من استخدم الرابط ومتى
- إحصائيات شاملة للاستخدام
```

### 4. لوحة تحكم المشرفين والمدراء
```
عند تسجيل الدخول، كل مستخدم يرى فقط البيانات الخاصة به:
- المشرف يرى المدارس المرتبطة به ويستطيع إضافة مدراء
- المدير يرى مدرسته فقط ويتعامل مع الأهالي
```

## 🚀 التشغيل والاختبار

### 1. تشغيل الخوادم
```bash
# Laravel Backend
php artisan serve
# يعمل على http://127.0.0.1:8000

# React Frontend  
cd frontend/my-project-main
npm start
# يعمل على http://localhost:3000
```

### 2. إعداد البيانات الأولية
```bash
php artisan setup:supervisor-system
```

### 3. بيانات الدخول للاختبار
```
Admin Account:
Email: admin@ruaa.com
Password: admin123
```

## 📋 روابط النظام

1. **تسجيل الدخول للأدمن**: `/login`
2. **إدارة الدعوات**: `/admin/supervisor-links` 
3. **تسجيل مشرف**: `/register/supervisor/{token}`
4. **تسجيل مدير مدرسة**: `/register/principal?supervisor_token={token}`

## 🎯 الميزات الرئيسية

### ✅ أمان التوكنز
- توكنز فريدة ومشفرة
- صلاحية زمنية قابلة للتحديد
- عدد استخدامات محدود
- تتبع كامل للاستخدام

### ✅ إدارة شاملة
- إحصائيات مفصلة
- تفعيل/تعطيل الروابط
- تتبع التسجيلات الحديثة
- واجهة سهلة الاستخدام

### ✅ تجربة مستخدم محسنة
- تصميم متجاوب
- رسائل خطأ واضحة
- تأكيدات النجاح
- تحقق من صحة البيانات

### ✅ هيكل قاعدة البيانات
- علاقات محكمة بين الجداول
- Foreign keys للحفاظ على سلامة البيانات
- فهرسة مناسبة للأداء
- أنواع بيانات محسنة

## 🔄 سير العمل الكامل

1. **Admin ينشئ رابط دعوة** → النظام يولد توكن فريد
2. **Admin يرسل الرابط للمشرف/المدير** → عبر البريد الإلكتروني أو أي وسيلة
3. **المدعو يفتح الرابط** → النظام يتحقق من صحة التوكن
4. **المدعو يملأ النموذج** → مع التحقق من صحة البيانات
5. **النظام يسجل البيانات** → مع حالة pending
6. **Admin/Supervisor يراجع الطلب** → موافقة أو رفض
7. **تفعيل الحساب** → المستخدم يمكنه تسجيل الدخول

## 🛡️ الأمان والحماية

- **Token Validation**: تحقق صارم من صحة التوكنز
- **Expiration Control**: انتهاء صلاحية زمنية
- **Usage Limits**: حد أقصى لعدد الاستخدامات
- **Role-based Access**: صلاحيات محددة حسب الدور
- **Data Validation**: تحقق من صحة البيانات المدخلة
- **SQL Injection Protection**: حماية من هجمات قواعد البيانات

## 📊 التقارير والإحصائيات

- إجمالي الروابط المنشأة
- الروابط النشطة/المعطلة
- الروابط المنتهية الصلاحية
- التسجيلات الحديثة
- معدل الاستخدام
- إحصائيات حسب النوع (مشرف/مدير مدرسة)

---

## 🎉 النظام جاهز للاستخدام!

تم تطوير نظام شامل ومتكامل لإدارة تسجيل المشرفين ومدراء المدارس بنجاح. النظام يوفر:

- **أمان عالي** في إدارة الدعوات
- **مرونة كاملة** في التحكم بالصلاحيات  
- **واجهة سهلة** للمستخدمين والمديرين
- **تتبع شامل** لجميع العمليات
- **قابلية التوسع** لمتطلبات مستقبلية