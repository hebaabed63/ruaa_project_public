# دليل التكامل الكامل - منصة رؤى التعليمية

## نظرة عامة
تم تنفيذ التكامل الكامل بين Frontend و Backend و Database لجميع أقسام المنصة.

## البنية التحتية للمشروع

### Backend (Laravel)
```
app/Http/Controllers/
├── Api/
│   ├── public/          # Controllers للصفحات العامة
│   │   ├── SchoolController.php
│   │   ├── AboutController.php
│   │   ├── ContactController.php
│   │   ├── RatingController.php
│   │   ├── ServiceController.php
│   │   └── FeedbackController.php
│   ├── Parent/          # Controllers لأولياء الأمور
│   │   ├── ParentDashboardController.php
│   │   ├── ParentProfileController.php
│   │   ├── ParentSettingsController.php
│   │   ├── ParentComplaintsController.php
│   │   ├── ParentReportsController.php
│   │   └── SchoolEvaluationController.php
│   └── Supervisor/      # Controllers للمشرفين
│       └── SupervisorDashboardController.php
└── AuthController.php
```

### Frontend (React)
```
src/
├── services/
│   ├── publicApiService.js      # خدمات API للصفحات العامة
│   ├── parentApiService.js      # خدمات API لأولياء الأمور
│   └── supervisorApiService.js  # خدمات API للمشرفين
├── pages/
│   ├── public/                  # صفحات الزوار
│   ├── dashboard/
│   │   ├── parents/             # لوحة أولياء الأمور
│   │   ├── Supervisors/         # لوحة المشرفين
│   │   └── school-manager/      # لوحة مدراء المدارس
│   └── auth/                    # صفحات المصادقة
└── hooks/
    └── usePageTitle.js          # Hook لإدارة عناوين الصفحات
```

## API Routes المتاحة

### 1. Public APIs (غير محمية)

#### المدارس
```
GET    /api/schools                 - قائمة المدارس
GET    /api/schools/{id}            - تفاصيل مدرسة
GET    /api/schools/best            - أفضل المدارس
GET    /api/schools/recent          - المدارس المضافة حديثاً
GET    /api/schools/search          - البحث في المدارس
GET    /api/schools/by-region       - المدارس حسب المنطقة
GET    /api/schools/regions         - قائمة المناطق
```

#### الإحصائيات
```
GET    /api/statistics/general      - الإحصائيات العامة
GET    /api/schools/statistics      - إحصائيات المدارس
```

#### التواصل
```
GET    /api/contact/info            - معلومات التواصل
POST   /api/contact                 - إرسال رسالة تواصل
```

#### التقييمات والمراجعات
```
GET    /api/ratings/criteria               - معايير التقييم
GET    /api/ratings/school/{schoolId}      - تقييمات مدرسة محددة
POST   /api/ratings                        - إضافة تقييم (يتطلب تسجيل الدخول)
```

#### الخدمات
```
GET    /api/services               - قائمة الخدمات
GET    /api/services/{id}          - تفاصيل خدمة
```

#### الآراء والاقتراحات
```
GET    /api/feedback               - عرض الآراء
POST   /api/feedback               - إرسال رأي
```

### 2. Parent APIs (محمية - role = 3)

#### اللوحة الرئيسية
```
GET    /api/parent/dashboard       - البيانات الرئيسية لولي الأمر
GET    /api/parent/schools         - مدارس الأبناء
```

#### الملف الشخصي
```
GET    /api/parent/profile         - بيانات الملف الشخصي
PUT    /api/parent/profile         - تحديث الملف الشخصي
POST   /api/parent/profile/avatar  - تحديث الصورة الشخصية
```

#### الإعدادات
```
GET    /api/parent/settings                    - الإعدادات
POST   /api/parent/settings/change-password    - تغيير كلمة المرور
PUT    /api/parent/settings/notifications      - تحديث إعدادات الإشعارات
```

#### التقييمات
```
GET    /api/parent/evaluation-criteria                - معايير التقييم
GET    /api/parent/schools/{schoolId}/evaluation      - تقييم مدرسة
POST   /api/parent/schools/{schoolId}/evaluation      - إرسال تقييم
```

#### الإشعارات
```
GET    /api/parent/notifications           - قائمة الإشعارات
PUT    /api/parent/notifications/{id}/read - تحديد إشعار كمقروء
```

#### التقارير
```
GET    /api/parent/reports         - قائمة التقارير
```

#### الشكاوى
```
GET    /api/parent/complaints/meta - البيانات الوصفية للشكاوى
GET    /api/parent/complaints      - قائمة الشكاوى
POST   /api/parent/complaints      - تقديم شكوى
```

### 3. Supervisor APIs (محمية - role = 1)

#### اللوحة الرئيسية
```
GET    /api/supervisor/dashboard/stats     - إحصائيات لوحة التحكم
GET    /api/supervisor/schools             - المدارس المشرف عليها
```

#### الملف الشخصي
```
GET    /api/supervisor/profile             - بيانات الملف الشخصي
PUT    /api/supervisor/profile             - تحديث الملف الشخصي
POST   /api/supervisor/profile/avatar      - تحديث الصورة الشخصية
```

#### التقارير
```
GET    /api/supervisor/reports             - قائمة التقارير
POST   /api/supervisor/reports             - إنشاء تقرير جديد
PUT    /api/supervisor/reports/{id}        - تحديث تقرير
DELETE /api/supervisor/reports/{id}        - حذف تقرير
```

#### الإشعارات
```
GET    /api/supervisor/notifications           - قائمة الإشعارات
PUT    /api/supervisor/notifications/{id}/read - تحديد إشعار كمقروء
```

#### الدعوات
```
GET    /api/supervisor/invitations         - قائمة الدعوات
POST   /api/supervisor/invitations         - إرسال دعوة جديدة
```

#### المحادثات
```
GET    /api/supervisor/conversations                   - قائمة المحادثات
GET    /api/supervisor/conversations/{id}/messages     - رسائل محادثة
POST   /api/supervisor/conversations/{id}/messages     - إرسال رسالة
```

## الميزات المنفذة

### 1. صفحات الزوار (Public Pages)
✅ صفحة رئيسية فعالة مع روابط صحيحة
✅ نماذج التواصل متصلة بالباك-إند
✅ عرض المدارس من قاعدة البيانات
✅ نظام التقييمات والمراجعات
✅ عناوين الصفحات مع اللوجو

### 2. لوحة تحكم أولياء الأمور
✅ عرض الإحصائيات من قاعدة البيانات
✅ عرض مدارس الأبناء
✅ تقييم المدارس
✅ الملف الشخصي الكامل (عرض + تعديل + صورة)
✅ نظام الإشعارات
✅ تقديم الشكاوى
✅ عرض التقارير
✅ الإعدادات وتغيير كلمة المرور

### 3. لوحة تحكم المشرفين
✅ إحصائيات فعلية من قاعدة البيانات
✅ عرض المدارس المشرف عليها
✅ الملف الشخصي (عرض + تعديل + صورة)
✅ إنشاء وإدارة التقارير
✅ نظام الإشعارات الحقيقية
✅ نظام المحادثات مع مدراء المدارس
✅ إرسال الدعوات للمدراء
✅ جميع التنقلات تعمل بشكل صحيح

### 4. عامة
✅ جميع الـ Controllers منظمة في مجلدات صحيحة
✅ API Services للـ Frontend
✅ usePageTitle hook لجميع الصفحات
✅ اللوجو والعنوان في كل صفحة
✅ التعامل مع الأخطاء والـ validation
✅ نظام المصادقة والتفويض

## كيفية تشغيل المشروع

### 1. Backend (Laravel)
```bash
# التأكد من تشغيل خادم قاعدة البيانات
# في terminal جديد
cd C:\laragon\www\ruaa_project

# تشغيل migrations (إذا لم تكن قد شغلتها)
php artisan migrate

# تشغيل Laravel server
php artisan serve
```

### 2. Frontend (React)
```bash
# في terminal جديد
cd C:\laragon\www\ruaa_project\frontend\my-project-main

# تثبيت المكتبات (إذا لم تكن مثبتة)
npm install

# تشغيل React
npm start
```

### 3. الوصول للتطبيق
```
Frontend: http://localhost:3000
Backend API: http://localhost:8000/api
```

## الصلاحيات (Roles)

| Role | القيمة | الوصف |
|------|-------|------|
| Admin | 0 | المدير العام |
| Supervisor | 1 | المشرف |
| School Manager | 2 | مدير المدرسة |
| Parent | 3 | ولي الأمر |

## ملاحظات هامة

1. **التصميم**: لم يتم تغيير أي ستايل أو تصميم في جميع الصفحات
2. **التنقل**: جميع الروابط تعمل وتوجه للصفحات الصحيحة
3. **البيانات**: جميع البيانات تأتي من قاعدة البيانات فعلياً
4. **الأمان**: جميع الـ APIs محمية بنظام Sanctum
5. **Validation**: جميع النماذج تحتوي على validation من Frontend و Backend

## الخطوات التالية (اختيارية)

1. إضافة المزيد من الـ seeds لبيانات تجريبية
2. تحسين رسائل الأخطاء
3. إضافة المزيد من الفلاتر في صفحات البحث
4. تحسين الأداء بإضافة caching
5. إضافة real-time notifications باستخدام Pusher/Laravel Echo

## الدعم الفني

للمشاكل أو الأسئلة:
- تأكد من تشغيل Laravel و React معاً
- تأكد من وجود قاعدة البيانات وتشغيل الـ migrations
- تحقق من ملف .env في Backend و Frontend

---

**تم التكامل بنجاح!** 🎉

جميع اللوحات والصفحات تعمل بشكل كامل مع Backend و Database دون أي تغيير في التصميم.
